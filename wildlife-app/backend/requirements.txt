# Core FastAPI and web server
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.6

# Database
sqlalchemy==2.0.23
psycopg2-binary==2.9.9

# Environment and configuration
python-dotenv==1.0.0

# Async file operations
aiofiles>=24.1.0

# HTTP requests
requests>=2.31.0

# Image processing
opencv-python>=4.8.0
pillow>=10.0.0
numpy>=1.24.0

# System monitoring
psutil>=5.9.0

# Wildlife classification
speciesnet[server]

# Optional: Advanced AI models (uncomment to enable)
ultralytics>=8.0.0  # For YOLOv8/YOLOv10/YOLOv11 support
transformers>=4.30.0  # For CLIP support
torch>=2.0.0  # Required for CLIP and advanced models

# Data validation (included with FastAPI but explicit for clarity)
pydantic>=2.0.0

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-asyncio>=0.21.0
pytest-mock>=3.12.0
httpx>=0.25.0  # For async HTTP testing

# Rate limiting
slowapi>=0.1.9

# Task scheduling
APScheduler>=3.10.0
bcrypt>=4.0.0
python-jose[cryptography]>=3.3.0

# SMS notifications
twilio>=8.10.0

# PDF generation
reportlab>=4.0.0

# Face Recognition (optional - automatically installs dlib)
# Install with: pip install face-recognition
face-recognition>=1.3.0

# MediaPipe for better face detection (optional, but recommended for improved accuracy)
# Install with: pip install mediapipe
mediapipe>=0.10.0

# Audio processing for sound detection (optional)
librosa>=0.10.0  # For audio processing and feature extraction
soundfile>=0.12.0  # For audio file I/O

# NLP for chat interface (optional - uses Hugging Face models)
transformers>=4.30.0  # Already above, but explicitly for chat NLP
torch>=2.0.0  # Already above, but explicitly for NLP models

# Optional: For better async database performance
# asyncpg>=0.29.0  # Uncomment if you want to use asyncpg instead of psycopg2
